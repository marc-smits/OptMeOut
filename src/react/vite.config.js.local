import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  base: './',
  plugins: [react()],
    server: {
    // Translate all local /api calls to http://localhost:8080/api
    proxy: {
      '/api':'http://localhost:8080/api',
    },
  },
})
